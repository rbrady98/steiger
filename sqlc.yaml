version: "2"
sql:
  - engine: "sqlite"
    schema: "./internal/database/migrations"  # Keep migrations centralized
    queries: "./internal/joke/queries.sql"
    gen:
      go:
        package: "db"
        out: "./internal/joke/db"
